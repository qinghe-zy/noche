# 任务卡：T-003 草稿槽位规则收敛与测试入口

## 1. 任务目标
以 `docs/tech/data_model.md` 为准，统一草稿槽位键生成规则，建立最小单元测试入口，防止后续页面、store、repository 写入不同草稿槽。

## 2. 派工安排
- **负责 AI**: Codex
- **写入边界**:
  - `src/domain/draft/**`
  - `src/shared/constants/**`
  - `src/app/store/draft.store.ts`
  - `tests/**`
  - 根目录测试配置
  - 本任务与 checkpoint 文档

## 3. 任务详情
- [x] 确认功能依据以 `docs/tech` 为准
- [x] 新增 `vitest` 与 `pnpm test:unit`
- [x] 新增草稿槽位规则单元测试
- [x] 先运行失败测试，确认旧规则存在命名漂移
- [x] 统一 `buildDraftSlotKey` 与 `buildDiaryDraftKey` 使用 `DRAFT_KEYS`
- [x] 旧版 draft store 的 `getDraftSlotKey` 复用 domain 规则
- [x] 验证单元测试通过
- [x] 验证类型检查通过
- [x] 验证 H5 构建通过
- [x] Git commit

## 4. 验收标准
- [x] 日记草稿槽位为 `draft_diary_YYYY-MM-DD`
- [x] 随笔草稿槽位为 `draft_jotting`
- [x] 未来信草稿槽位为 `draft_future`
- [x] `pnpm test:unit` 通过
- [x] `pnpm type-check` 通过
- [x] `pnpm build:h5` 通过

## 5. 风险与注意事项
- 当前只收敛底层规则，不实现正式页面。
- `vitest` 当前仅覆盖 domain 规则，后续 repository/store 闭环需要继续补测试。
