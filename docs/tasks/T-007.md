# 任务卡：T-007 Gemini Home UI 实现与 Codex 验收

## 1. 任务目标
按 T-006 handoff 显式调用 Gemini CLI，让 Gemini 在 `src/features/home/**` 实现首页 UI；Codex 负责集成修正、验证与提交。

## 2. 派工安排
- **Gemini 写入边界**:
  - `src/features/home/**`
- **Codex 收口边界**:
  - 构建兼容修正
  - favicon / 验收辅助
  - 记录、checkpoint、commit

## 3. Gemini 调用
命令形态：

```powershell
gemini --approval-mode auto_edit -p "...read HANDOFF_07 and implement Home UI only under src/features/home/**..."
```

结果：
- Gemini CLI 成功运行
- Gemini 修改 Home 页面并新增 Home-local components
- Gemini 最后尝试调用自身不存在的 `run_shell_command` 工具，验证由 Codex 接手完成

## 4. Codex 集成修正
- [x] 将 `lang="scss"` 改为纯 CSS，避免新增 `sass`
- [x] 将嵌套选择器改为普通 CSS 选择器
- [x] 移除 footer emoji 图标，改为文字圆形标记
- [x] 移除负字距
- [x] 新增 `public/favicon.svg`
- [x] 在 `index.html` 添加 favicon link

## 5. 验收标准
- [x] Gemini 只改 Home feature 目录
- [x] Codex 未改动 domain/data/store 规则
- [x] `pnpm test:unit` 通过
- [x] `pnpm type-check` 通过
- [x] `pnpm build:h5` 通过
- [x] Playwright 移动视口首页非空
- [x] Playwright 控制台 0 error
- [x] `Write Today` 可跳转 Editor diary 路由
- [x] Git commit

## 6. 风险与注意事项
- 当前 Home 仍是 UI 入口，不接真实编辑状态。
- 后续 editor/draft 写作闭环应先由 Codex 稳定接口，再交给 Gemini 做页面表达。
