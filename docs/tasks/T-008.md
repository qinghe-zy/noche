# 任务卡：T-008 editor / draft / save / read 最小闭环

## 1. 任务目标
在 SQLite 真实接入前，基于内存 draft repository + draft store + entry store，打通 editor 页的最小可运行闭环：打开草稿、持续保存草稿、正式保存为 entry、在同页进入可读状态。

## 2. 派工安排
- **Codex 写入边界**:
  - `src/domain/**`
  - `src/data/repositories/**`
  - `src/app/store/**`
  - `tests/**`
  - `docs/tasks/T-008.md`
  - `docs/handoffs/HANDOFF_08_Codex_to_Gemini_editor_ui.md`
  - 本轮 checkpoint / progress / findings / task_plan
- **Gemini 写入边界**:
  - `src/features/editor/**`

## 3. 任务详情
- [x] 先写失败测试覆盖 draft repository 的保存 / 读取 / 删除
- [x] 先写失败测试覆盖 draft store 的 open / save / restore / remove
- [x] 先写失败测试覆盖 draft 正式保存为 entry 时写入 `savedAt`
- [x] 新增 `IDraftRepository` 与内存版 `createMemoryDraftRepository`
- [x] 更新 `useDraftStore.ts`，支持 repository 注入与最小闭环 action
- [x] 新增 `createEntryFromDraft`
- [x] 写入轻量 Gemini handoff，要求其只实现 editor 页面表达
- [x] 显式调用 Gemini CLI 落地 `src/features/editor/**`
- [x] Gemini CLI 两次 headless 调用超时且未落盘；Codex 接手本轮 editor 页面实现与验收
- [x] 更新 checkpoint / task / progress / findings
- [x] Git commit

## 4. 验收标准
- [x] `useDraftStore` 不再只是内存字典占位
- [x] draft 可按槽位 restore
- [x] 正式保存 entry 时 `savedAt` 不为空
- [x] editor 页面不再是占位壳
- [x] diary / jotting / future 都能进入 editor
- [x] 页面可在同页形成最小 save/read 闭环
- [x] `pnpm test:unit` 通过
- [x] `pnpm type-check` 通过
- [x] `pnpm build:h5` 通过

## 5. 风险与注意事项
- 当前 draft repository 仍是内存实现，只保证本轮运行态闭环，不代表最终 SQLite 持久化完成。
- Gemini 只负责 `src/features/editor/**`，不得改动 domain / data / store / 根配置。
- 页面保持纯 CSS，不新增依赖，不引入 SCSS。
- Gemini CLI 当前 headless `-p` 在本仓库里两次超时且未写文件；后续若继续交给 Gemini，建议先缩小写入面并显式观察其 session 行为。
