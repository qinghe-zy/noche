# 任务卡：T-004 核心模型按 tech 最终口径收敛

## 1. 任务目标
以 `docs/tech` 为功能依据，将核心 Entry / Draft 类型和数据映射从旧口径 `future-letter` / `futureStatus` / `futureUnlockDate` 收敛到最终 tech 口径 `future` / `EntryStatus` / `unlockDate`。

## 2. 派工安排
- **负责 AI**: Codex
- **写入边界**:
  - `src/domain/**`
  - `src/data/**`
  - `src/shared/constants/**`
  - `tests/**`
  - `docs/tech/data_model.md`
  - 本任务与 checkpoint 文档

## 3. 任务详情
- [x] 读取 `docs/tech` 最终版模型文档并确认冲突点
- [x] 先写失败测试锁定 `future` 与 `EntryStatus` 口径
- [x] 更新 `EntryType` 为 `diary | jotting | future`
- [x] 新增 `EntryStatus = saved | sealed | unlocked`
- [x] 将未来信日期字段统一为 `unlockDate`
- [x] 更新 entry mapper / record / schema 字段
- [x] 更新 draft slot rule 对 `future` 的识别
- [x] 更新 tracked `docs/tech/data_model.md`
- [x] 验证单元测试通过
- [x] 验证类型检查通过
- [x] 验证 H5 构建通过
- [x] Git commit

## 4. 验收标准
- [x] 代码中不再使用 `future-letter`
- [x] 代码中不再使用 `futureStatus`
- [x] 代码中不再使用 `futureUnlockDate`
- [x] `pnpm test:unit` 通过
- [x] `pnpm type-check` 通过
- [x] `pnpm build:h5` 通过

## 5. 风险与注意事项
- `docs/tech/*_final.md` 当前仍为未跟踪文件，本轮不提交重复真相文档。
- 下一轮 repository/store 闭环必须继续以 `future/status/unlockDate` 为准。
