# 任务卡：T-005 entry repository + store 最小闭环

## 1. 任务目标
在 SQLite 真实接入前，基于 `IEntryRepository` 提供可执行内存仓储，并让 `useEntryStore` 通过 repository 完成保存、按日期读取和销毁，形成可测试的底层闭环。

## 2. 派工安排
- **负责 AI**: Codex
- **写入边界**:
  - `src/data/repositories/**`
  - `src/app/store/**`
  - `tests/app/**`
  - `tests/data/**`
  - 本任务与 checkpoint 文档

## 3. 任务详情
- [x] 先写失败测试覆盖内存仓储行为
- [x] 先写失败测试覆盖 entry store 通过 repository 保存 / 读取 / 销毁
- [x] 先写失败测试覆盖 legacy store 文件不重复 define Pinia id
- [x] 新增 `createMemoryEntryRepository`
- [x] 更新 `useEntryStore` 支持 repository 注入
- [x] 将 `entry.store.ts` 改为 `useEntryStore.ts` alias
- [x] 将 `draft.store.ts` 改为 `useDraftStore.ts` alias，并保留 `getDraftSlotKey`
- [x] 验证 app/store 下只剩 canonical store 文件直接 `defineStore`
- [x] 验证单元测试通过
- [x] 验证类型检查通过
- [x] 验证 H5 构建通过
- [x] Git commit

## 4. 验收标准
- [x] `createMemoryEntryRepository` 可 save / query / delete
- [x] 日历标记只包含 diary / jotting，不包含 future
- [x] `useEntryStore` 不再只是同步占位逻辑
- [x] legacy `.store.ts` 文件不再重复定义 Pinia store
- [x] `pnpm test:unit` 通过
- [x] `pnpm type-check` 通过
- [x] `pnpm build:h5` 通过

## 5. 风险与注意事项
- 内存仓储不是最终 SQLite 实现，只用于当前阶段打通可运行闭环和测试。
- 下一轮给 Gemini 的 UI handoff 只交付可用 store/repository 接口，不限制视觉创意。
