# 任务卡：T-001 工程初始化

## 1. 任务目标
确认技术栈底座（Uni-app CLI + Vue 3 + TS + Pinia），验证工程可编译可运行，规范化目录结构。

## 2. 派工安排
- **负责 AI**: Codex
- **写入边界**: 根目录配置文件, `src/**` (骨架)

## 3. 任务详情
- [x] 确认 `package.json` 依赖完整
- [x] 确认 `vite.config.ts` 别名配置 (`@/`)
- [x] 确认 `tsconfig.json` 类型支持
- [x] 验证 H5 编译流程 (`npm run build:h5`)
- [x] 建立/确认分层目录 (`app`, `domain`, `data`, `features`, `shared`)
- [x] 写入 CHECKPOINT_01_init_ready.md
- [ ] Git commit

## 4. 验收标准
- [x] `npm run build:h5` 执行成功无 error
- [x] 基础路径别名 `@/` 正常解析
- [x] 页面路由 `pages.json` 与 `features/` 占位页匹配

## 5. 风险与注意事项
- 暂不处理 Android 真机调试工具链
