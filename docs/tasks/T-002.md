# 任务卡：T-002 domain + data 骨架

## 1. 任务目标
建立核心领域模型类型、数据访问接口、Pinia store 骨架及共享工具函数，为后续业务实现提供完整的类型基线。

## 2. 派工安排
- **负责 AI**: Antigravity（直接执行，无 codexcli/geminicli 可调用）
- **写入边界**:
  - `src/domain/**`
  - `src/data/**`
  - `src/app/store/**`
  - `src/shared/**`

## 3. 任务详情
- [x] 定义 `src/domain/entry/types.ts`（EntryType, FutureLetterStatus, Entry 接口）
- [x] 定义 `src/domain/draft/types.ts`（DraftType, Draft, DraftEntry 接口）
- [x] 补充 `src/domain/time/utils.ts`（getTodayDate, isFutureLetterUnlockable 等工具方法）
- [x] 创建 `src/data/repositories/entry.repository.ts`（IEntryRepository 接口）
- [x] 创建 `src/data/db/database.interface.ts`（IDatabase 接口 + NoopDatabase 实现）
- [x] 创建 `src/app/store/entry.store.ts`（Pinia 骨架）
- [x] 创建 `src/app/store/draft.store.ts`（Pinia 骨架 + uni.Storage 暂存）
- [x] 创建 `src/shared/constants/draftKeys.ts`（草稿槽位常量）
- [x] 修复 `src/shared/utils/id.ts`，补出 `createId` 别名
- [x] 修复类型对齐（Entry 时间字段对齐为 ISO string，兼容现有 mapper/service）
- [x] 修复 `src/data/mappers/entryMapper.ts` null 兼容问题
- [x] typescript 类型检查通过
- [x] 写入 CHECKPOINT_02_domain_data_ready.md
- [x] git commit

## 4. 验收标准
- [x] `npx vue-tsc --noEmit` 零错误
- [x] Entry / Draft 类型与 docs/tech/data_model.md 对齐
- [x] 草稿槽位规则已在 draftKeys.ts 和 draft.store.ts 中体现

## 5. 风险与注意事项
- codexcli / geminicli 在该系统不可用
- 所有操作由 Antigravity 直接执行
